language: haxe

os: linux
dist: trusty

haxe:
  - development
  - "3.4.4"

matrix:
  allow_failures:
    - haxe: development

install:
  - yes | haxelib install all
  - pushd samples
  - yes | haxelib install all
  - popd
  - haxelib dev heaps .
  - haxelib list

script:
  - haxe all.hxml
  - haxe --cwd samples all.hxml
